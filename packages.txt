# specific versioning not working atm
vscode
tailscale
visualstudio2019buildtools # --version=16.11.15.0
visualstudio2019-workload-vctools # --version=1.0.1
nodejs # --version=16.14.2
pyenv-win
make  # use in git-bash
git.install
linkshellextension
7zip
notepadplusplus # open large csv files
rustup.install
llvm # nautilus needs clang to build
rsync
lockhunter
everything

docker-cli
docker-machine
docker-compose
docker-desktop

# for debugging on thomas comp
# neovim
# lazygit

# es # everything CLI
# take out for now until recreating config
# vivaldi.install
# razer-synapse-3
# slack
# memurai-developer.install
# python@3.10 # using pyenv instead
# docker-desktop
# needed to install rust targets: rust not needed if rustup is installed. rustup target add x86_64-pc-windows-msvc
# use with act. failed one first install, re-run this script again
# act-cli
# directoryopus # view file sizes
# autohotkey
# resilio-sync-home
# googlechrome
# wsl2 # needed for docker desktop to use wsl
# github-desktop
# firefox
# pycharm
# microsoft-windows-terminal
# rust # using rustup instead, dont' have both.
# cmake
